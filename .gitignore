# secrets — never commit
.env
.claude/settings.local.json

# caches / derived
blog_cache/
__pycache__/
*.pyc
card_cache/

# os / editor junk
.DS_Store
.Rhistory

# stray local duplicate, not part of the project
retrieval_spike copy.py

# build output (regenerate with build_static.py)
dist/
